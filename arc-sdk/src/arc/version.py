"""ARC — single source of truth for the package version.

The value defined here is re-exported as ``arc.__version__`` and is the
variable bumped by the release tooling configured in ``pyproject.toml``.
"""

__version__: str = "0.1.0"